extends ../base

block content
  main.main
    input#email-confirm-token(type='hidden', name='token', value=`${token}`)
    .email-confirm
      p.email-confirm__text Please wait while we activate your account.

block append content
  script(src='https://cdnjs.cloudflare.com/ajax/libs/axios/0.19.0/axios.min.js')
  script(src='/js/emailConfirm.js')